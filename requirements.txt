# Install PyTorch separately first. This file intentionally excludes torch.
#
# Current benchmark stack, pinned to the local `megatron` conda environment.
# Megatron is installed via the published `megatron-core` package, while Apex
# still needs to be pinned to a repository commit.
#
# Typical usage:
#   export NVTE_FRAMEWORK=pytorch
#   export APEX_CPP_EXT=1
#   export APEX_CUDA_EXT=1
#   pip install -r requirements.txt
#
# If flash-attn requires a non-isolated build in your environment:
#   pip install --no-build-isolation flash-attn

packaging==25.0
psutil==7.1.3
ninja==1.13.0
pybind11==3.0.1
triton==3.3.1
transformer-engine[pytorch]==2.10.0
git+https://github.com/fanshiqing/grouped_gemm.git@172fada89fa7364fe5d026b3a0dfab58b591ffdd
megatron-core==0.16.0
git+https://github.com/NVIDIA/apex.git@a7b872ed8514295fae16a6df894b5ffab298a008
deepspeed==0.18.8
flash-attn==2.8.3
tutel @ https://github.com/microsoft/Tutel/archive/refs/tags/v0.4.1.tar.gz
